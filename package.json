{
  "name": "gmaps-leads-scraper",
  "version": "1.0.0",
  "description": "Extensión Chrome + backend Node para scrapear Google Maps sin ban, guardar leads en MongoDB y disparar campañas WhatsApp",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run bot:dev\"",
    "server:dev": "nodemon server/index.js",
    "bot:dev": "nodemon bot/index.js",
    "build": "npm run extension:build",
    "extension:build": "cd extension && npm run build",
    "rotate": "bash wg/rotate.sh",
    "scrape": "echo 'Inicia la extensión Chrome y navega a Google Maps'",
    "install:all": "npm install && cd extension && npm install && cd ../server && npm install && cd ../bot && npm install",
    "start": "concurrently \"npm run server:start\" \"npm run bot:start\"",
    "server:start": "node server/index.js",
    "bot:start": "node bot/index.js"
  },
  "keywords": [
    "google-maps",
    "scraper",
    "leads",
    "whatsapp",
    "mongodb"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.2"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "dotenv": "^16.3.1"
  }
}
